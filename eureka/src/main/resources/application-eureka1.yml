eureka:
  client:
    service-url:
      defaultZone: http://eureka2:8762/eureka/
#  此服务不注册到eureka上
    register-with-eureka: false
  server:
# 默认true,false指关闭调试模式
    enable-self-preservation: false
spring:
  application:
    name: eureka
management:
  server:
    servlet:
      context-path: /
server:
  port: 8761